#include "main.h"

/**
 * mul - multiply two numbers
 * @a: the first number to multiply
 * @b: the second number to be multiplied to first number
 * Return: the result of multiplication between a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
